{
  "name": "@winterfell/database",
  "version": "1.0.0",
  "type": "module",
  "exports": {
    ".": "./src/client.ts"
  },
  "scripts": {
    "db:migrate:deploy": "prisma migrate deploy",
    "db:migrate:dev": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:seed": "tsx src/seed.ts",
    "dev": "tsup src/* --watch",
    "format": "prisma format",
    "generate": "prisma generate",
    "prebuild": "npm run generate",
    "predev": "npm run generate",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "7.0.0",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@winterfell/eslint-config": "*",
    "@winterfell/typescript-config": "*",
    "eslint": "^9.32.0",
    "prisma": "7.0.0",
    "rimraf": "^5.0.5",
    "tsup": "^8.0.2",
    "tsx": "4.19.1",
    "typescript": "5.5.4"
  }
}
